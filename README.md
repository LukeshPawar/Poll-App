# Poll App

A modern, responsive **real-time polling/voting application** built with **React** + **TypeScript**.

Create polls quickly, share them with friends, colleagues or communities, and see live vote results update in real time.


## ✨ Features

- Create custom polls with multiple answer options
- Single-choice or multiple-choice voting (configurable)
- Real-time vote counting & live result updates
- Clean and responsive design (mobile + desktop friendly)
- Shareable poll links
- Optional: User authentication (if implemented)
- Optional: Poll expiration / closing mechanism
- Beautiful result visualization (pie/bar charts)

## 🛠️ Tech Stack

- **Frontend**: React (Vite / CRA)
- **Language**: TypeScript
- **Styling**: CSS Modules / Tailwind CSS / Styled Components (update according to your choice)
- **State Management**: React Context / Zustand / Redux Toolkit (if used)
- **Real-time Backend** (if applicable): Firebase Realtime Database / Firestore / Supabase / Socket.io
- **Charts**: Recharts / Chart.js / ApexCharts
- **Routing**: React Router DOM
- **Build Tool**: Vite (recommended) or Create React App

### Prerequisites

- Node.js ≥ 18
- npm / yarn / pnpm

### Installation

```bash
# Clone the repository
git clone https://github.com/LukeshPawar/Poll-App.git
cd Poll-App/poll-app

# Install dependencies
npm install
# or
yarn install
# or
pnpm install
